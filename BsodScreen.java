# BSOD Overlay Fabric 1.21.11

Клиентский Fabric-мод для Minecraft Java 1.21.11.

- `E` открывает фальшивый синий экран смерти внутри Minecraft.
- `0` или `Numpad 0` закрывает экран и возвращает игру обратно.
- Это не настоящий BSOD и не крашит систему. Просто экран в Minecraft, потому что человечество всё ещё требует пранки с синим фоном.

## Требования

- Minecraft Java `1.21.11`
- Fabric Loader `0.18.1+`, протестируйте с `0.19.2`
- Fabric API `0.141.4+1.21.11`
- JDK `21`
- Gradle `9.2+`

## Сборка

В папке проекта выполните:

```bash
gradle build
```

Готовый `.jar` появится в:

```text
build/libs/bsod-overlay-1.0.0.jar
```

Положите `.jar` в папку `mods` клиента вместе с Fabric API.

## Примечание про клавишу E

`E` уже занята ванильным инвентарём. Мод всё равно перехватывает нажатие и открывает BSOD-экран поверх, но в настройках управления Minecraft покажет конфликт клавиш. Это нормально. Мир как-то переживёт ещё один конфликт клавиш.
